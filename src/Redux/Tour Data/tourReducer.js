const initialState = {
    counter: 10,
  };
  const tourReducer = (state = initialState, { type, payload }) => {
    switch (type) {
      default:
        return state;
    }
  };
  
  export { tourReducer };
  