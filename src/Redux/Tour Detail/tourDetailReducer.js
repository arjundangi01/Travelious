const initialState = {
  counter: 10,
};
const tourDetailReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export { tourDetailReducer };
