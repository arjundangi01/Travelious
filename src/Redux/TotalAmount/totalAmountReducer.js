const initialState = {
  counter: 10,
};
const totalAmountReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export { totalAmountReducer };
